module "ecs_foobar" {
    source = "../"

    foo = "bar"
    bar = "foo"
}

module "ecs_Foobar" {
    source = "../"

    foo = "bar"
    bar = "foo"
}

                module "Ec-bar" {           
    source = "../"

    foo = "bar"
    bar = "foo"
}

    module "ecs_barfoo" {
    source = "../"

    foo = "bar"
    bar = "foo"
}

    module "ecs_foo" {
        source = "../"

        foo = "bar"
        bar = "foo"
    }

module "ecs_bar" {
source = "../"

foo = "bar"
bar = "foo"
}

module "ecs_bar_virginia" {
source = "../"

foo = "bar"
bar = "foo"
}

                    module "ecs_foo_virginia" {
source = "../"

foo = "bar"
bar = "foo"
}

module "virginia" {
    source = "../"

    foo = "bar"
    bar = "foo"
}

module ecs_virginia {
    source = "../"

    foo = "bar"
    bar = "foo"
}

module ecs_bad {
    source = "../"

    foo = "bar"
    bar = "foo"
}
